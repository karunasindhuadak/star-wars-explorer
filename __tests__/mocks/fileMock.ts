// When Jest encounters: import logo from "./logo.png"
// It can't load images, so this mock returns a dummy string instead.
// Your component gets "test-file-stub" as the image src, which is harmless.

export default "test-file-stub";
